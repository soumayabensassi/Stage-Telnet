<form method="post" action="{{ path('app_domaine_application_delete', {'id': domaine_application.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ domaine_application.id) }}">
    <button class="btn">Delete</button>
</form>
